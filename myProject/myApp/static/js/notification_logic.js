// // Function to update the notification count badge

// function updateNotificationCount() {
//     const notificationCountBadge = document.getElementById('notificationCount');
//     const notificationIcon = document.getElementById('notificationIcon'); // Get the notification icon element

//     if (unreadNotificationsCount > 0) {
//         notificationCountBadge.textContent = unreadNotificationsCount;
//         notificationCountBadge.style.display = 'inline-block'; // Show the badge
//         notificationIcon.src = "/static/img/notification_with_count.png"; // Change the icon to indicate notifications
//     } else {
//         notificationCountBadge.style.display = 'none'; // Hide the badge if count is zero
//         notificationIcon.src = "/static/img/notification.png"; // Change back to the default icon without count
//     }
// }

